/**
 * Minimum consolidated workdays required before a ranking % or period comparison
 * is treated as reliable. Below this sample, UI/API should surface "muestra insuficiente".
 */
export const STATISTICS_MIN_SAMPLE_WORKDAYS = 3;
