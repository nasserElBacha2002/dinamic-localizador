/** Distributed lock resource for recurring workday materialization (sp_getapplock Session). */
export const RECURRING_WORKDAY_MATERIALIZATION_LOCK_RESOURCE =
  "recurring-workday-materialization";

/** Distributed lock for attendance reminder scheduler tick (sp_getapplock Session). */
export const ATTENDANCE_REMINDER_JOB_LOCK_RESOURCE = "attendance-reminder-job";
